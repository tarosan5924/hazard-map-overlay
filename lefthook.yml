pre-commit:
  parallel: true
  jobs:
    - name: biome-check
      glob: "*.{js,ts,json}"
      run: pnpm biome check --write --no-errors-on-unmatched {staged_files}
      stage_fixed: true
    - name: test
      run: pnpm test

pre-push:
  jobs:
    - name: test
      run: pnpm test
